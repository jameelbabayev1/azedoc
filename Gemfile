source 'https://rubygems.org'

ruby '2.6.0'

# Standard library additions - optional for Ruby 2.6+
# All dependencies are from Ruby standard library (webrick, json, net/http, uri, base64, digest, time)
# No external gems required for core functionality

group :development do
  gem 'sinatra', '~> 2.0' # Optional: if you want to upgrade to Sinatra in future
end
